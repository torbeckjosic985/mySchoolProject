function getRandomInt(max) {
  return Math.floor(Math.random() * max);
}

// Usage example
let randomNumber = getRandomInt(10); // returns a random integer between 0 and 9, inclusive of 0 and exclusive of 10
